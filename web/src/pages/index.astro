---
import { Image } from "astro:assets";
import Barcode from "../assets/barcode.svg";
import RightComponent from "./RightComponent.svelte";
---

<html lang="en">
    <head>
        <meta charset="utf-8" />
        <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
        <meta name="viewport" content="width=device-width" />
        <meta name="generator" content={Astro.generator} />
        <link rel="stylesheet" href="/src/styles.css" />
        <title>UaaS</title>
    </head>
    <body class="h-screen flex p-2 lg:p-16 gap-16 justify-center">
        <div
            class="flex flex-col lg:flex-row gap-32 lg:gap-16 w-full max-w-[1312px]"
        >
            <!-- left -->
            <div class="w-full h-full flex flex-col gap-48">
                <div class="h-full flex flex-col gap-12 lg:gap-48">
                    <h1 class="text-8xl">UaaS</h1>
                    <span class="text-[14px]">functional satire by rew</span>
                </div>

                <div class="h-full border-t-2 border-t-black pt-4">
                    <p>
                        Why write the same code again and again, when you can
                        make a network call?
                    </p>
                </div>
            </div>

            <RightComponent client:load>
                <Image
                    slot="barcode"
                    src={Barcode}
                    alt="Utilities as a Service barcode"
                />
            </RightComponent>
        </div>
    </body>
</html>
